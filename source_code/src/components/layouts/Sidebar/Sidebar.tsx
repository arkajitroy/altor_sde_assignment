import React from "react";
import { TSidebarProps } from "../../../@types/TSidebar.types";

const Sidebar: React.FC<TSidebarProps> = ({ showSidebar }): JSX.Element => {
  return <div>Sidebar</div>;
};

export default Sidebar;
