import React from "react";
import { TNavbarProps } from "../../../@types/TNavbar.types";

import "./navbar.scss";

const Navbar: React.FC<TNavbarProps> = ({ setShowSidebar }): JSX.Element => {
  return <div>Navbar</div>;
};

export default Navbar;
